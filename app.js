"use strict";

/* LOCKED OS drop-in app.js.
   Loads the pinned original app.js, then installs the Supabase reliability fix
   inside the same global script environment. */
(() => {
  const originalUrl = "https://cdn.jsdelivr.net/gh/policypal1/LOCKED-OS@48b687462247c682f2762c86028fdc0f21030f61/app.js";
  try {
    const request = new XMLHttpRequest();
    request.open("GET", originalUrl, false);
    request.send(null);

    if (request.status < 200 || request.status >= 300) {
      throw new Error(`HTTP ${request.status}`);
    }

    const source = request.responseText + "\n" + "\n;(() => {\n  \"use strict\";\n\n  const LOCKED_SYNC_PATCH_VERSION = 2;\n  let lockedSyncRetryTimer = null;\n  let lockedSyncRetryDelay = 2000;\n\n  function lockedSyncTimestamp(snapshot = state) {\n    const value = snapshot?.meta?.syncClientUpdatedAt;\n    return typeof value === \"string\" && value ? value : \"\";\n  }\n\n  function lockedSyncActivityScore(snapshot) {\n    if (!snapshot || typeof snapshot !== \"object\") return 0;\n    let score = 0;\n\n    const meta = snapshot.meta || {};\n    score += Object.keys(meta.looksTaskEdits || {}).length * 10;\n    score += (Array.isArray(meta.looksCustomTasks) ? meta.looksCustomTasks.length : 0) * 15;\n    score += (Array.isArray(meta.looksDeletedTaskIds) ? meta.looksDeletedTaskIds.length : 0) * 5;\n    score += Object.keys(meta.taskInfo || {}).length * 4;\n    score += (Array.isArray(meta.gymScheduleChanges) ? meta.gymScheduleChanges.length : 0) * 3;\n\n    for (const day of Object.values(snapshot.days || {})) {\n      if (!day || typeof day !== \"object\") continue;\n      score += (Array.isArray(day.done) ? day.done.length : 0);\n      score += (Array.isArray(day.looksDone) ? day.looksDone.length : 0);\n      score += (Array.isArray(day.looksSkipped) ? day.looksSkipped.length : 0) * 0.5;\n      score += Math.min(3, Math.max(0, Number(day.waterOz) || 0) / 40);\n    }\n\n    score += Object.keys(snapshot.weights || {}).length * 0.5;\n    return score;\n  }\n\n  function lockedSyncMarkLocalChange() {\n    state.meta = state.meta && typeof state.meta === \"object\" ? state.meta : {};\n    state.meta.syncClientUpdatedAt = new Date().toISOString();\n    state.meta.syncPatchVersion = LOCKED_SYNC_PATCH_VERSION;\n  }\n\n  function lockedSyncScheduleRetry() {\n    clearTimeout(lockedSyncRetryTimer);\n    if (typeof navigator !== \"undefined\" && navigator.onLine === false) return;\n    lockedSyncRetryTimer = setTimeout(() => {\n      lockedSyncRetryTimer = null;\n      queueSupabaseSave(0);\n    }, lockedSyncRetryDelay);\n    lockedSyncRetryDelay = Math.min(30000, Math.round(lockedSyncRetryDelay * 1.7));\n  }\n\n  function lockedSyncPreferLocal(localSnapshot, remoteState, remoteUpdatedAt = \"\") {\n    const localStamp = lockedSyncTimestamp(localSnapshot);\n    const remoteStamp = remoteUpdatedAt || lockedSyncTimestamp(remoteState);\n\n    if (localStamp && remoteStamp) return localStamp > remoteStamp;\n    if (localStamp && !remoteStamp) return true;\n    if (!localStamp && remoteStamp) return false;\n\n    // Migration path for old saves that do not yet have timestamps:\n    // keep the richer snapshot so current local checkmarks/customizations are not lost.\n    return lockedSyncActivityScore(localSnapshot) > lockedSyncActivityScore(remoteState);\n  }\n\n  saveState = function() {\n    lockedSyncMarkLocalChange();\n    localRevision += 1;\n    saveLocalState();\n    queueSupabaseSave(40);\n  };\n\n  queueSupabaseSave = function(delay = 40) {\n    clearTimeout(saveTimer);\n    saveTimer = setTimeout(() => {\n      saveTimer = null;\n      saveSupabaseState();\n    }, Math.max(0, Number(delay) || 0));\n  };\n\n  saveSupabaseState = async function() {\n    if (!supabaseClient) {\n      if (syncStatus) syncStatus.textContent = \"Saved locally. Supabase is not connected.\";\n      return false;\n    }\n\n    if (supabaseSaveInFlight) {\n      supabaseSaveQueued = true;\n      return false;\n    }\n\n    supabaseSaveInFlight = true;\n    supabaseSaveQueued = false;\n\n    if (!lockedSyncTimestamp(state)) {\n      lockedSyncMarkLocalChange();\n      localRevision = Math.max(1, localRevision);\n      saveLocalState();\n    }\n\n    const revisionToSave = Math.max(1, localRevision);\n    const snapshot = JSON.parse(JSON.stringify(state));\n    const writeTimestamp = lockedSyncTimestamp(snapshot) || new Date().toISOString();\n    let saveSucceeded = false;\n\n    if (syncStatus) syncStatus.textContent = \"Saving to Supabase\u2026\";\n\n    try {\n      const { error } = await supabaseClient.from(SUPABASE_TABLE).upsert({\n        id: SUPABASE_ROW_ID,\n        state: snapshot,\n        updated_at: writeTimestamp\n      });\n\n      if (error) {\n        console.error(\"Supabase save failed:\", error);\n        if (syncStatus) syncStatus.textContent = \"Supabase save failed \u2014 retrying\u2026\";\n        lockedSyncScheduleRetry();\n        return false;\n      }\n\n      syncedRevision = Math.max(syncedRevision, revisionToSave);\n      latestSupabaseWriteAt = writeTimestamp;\n      lockedSyncRetryDelay = 2000;\n      clearTimeout(lockedSyncRetryTimer);\n      lockedSyncRetryTimer = null;\n      saveSucceeded = true;\n\n      if (syncStatus) {\n        syncStatus.textContent = localRevision > syncedRevision\n          ? \"Saving newer changes\u2026\"\n          : \"Saved to Supabase \u2713\";\n      }\n      return true;\n    } catch (error) {\n      console.error(\"Supabase save threw an error:\", error);\n      if (syncStatus) syncStatus.textContent = \"Supabase unavailable \u2014 retrying\u2026\";\n      lockedSyncScheduleRetry();\n      return false;\n    } finally {\n      supabaseSaveInFlight = false;\n      if (saveSucceeded && (supabaseSaveQueued || localRevision > syncedRevision)) {\n        queueSupabaseSave(0);\n      }\n    }\n  };\n\n  loadSupabaseState = async function() {\n    if (!supabaseClient) {\n      if (syncStatus) syncStatus.textContent = \"Saved locally. Supabase is not connected.\";\n      return;\n    }\n\n    const localBeforeLoad = JSON.parse(JSON.stringify(state));\n    const remoteRow = await fetchSupabaseState();\n\n    if (remoteRow?.state && typeof remoteRow.state === \"object\") {\n      if (lockedSyncPreferLocal(localBeforeLoad, remoteRow.state, remoteRow.updated_at || \"\")) {\n        state = localBeforeLoad;\n        normalizeState();\n        localRevision = Math.max(1, localRevision);\n        saveLocalState();\n        const ok = await saveSupabaseState();\n        if (ok && syncStatus) syncStatus.textContent = \"Local changes pushed to Supabase \u2713\";\n      } else {\n        applyRemoteState(\n          remoteRow.state,\n          \"Synced with Supabase.\",\n          { force: true, updatedAt: remoteRow.updated_at || \"\" }\n        );\n      }\n    } else {\n      state = localBeforeLoad;\n      normalizeState();\n      localRevision = Math.max(1, localRevision);\n      saveLocalState();\n      await saveSupabaseState();\n    }\n\n    subscribeToSupabaseState();\n  };\n\n  refreshSupabaseState = async function() {\n    if (!supabaseClient || mainApp.classList.contains(\"hidden\")) return;\n    if (saveTimer || supabaseSaveInFlight) return;\n\n    const localSnapshot = JSON.parse(JSON.stringify(state));\n    const remoteRow = await fetchSupabaseState({ silent: true });\n\n    if (!remoteRow?.state || typeof remoteRow.state !== \"object\") {\n      if (localRevision > syncedRevision || lockedSyncTimestamp(localSnapshot)) queueSupabaseSave(0);\n      return;\n    }\n\n    if (lockedSyncPreferLocal(localSnapshot, remoteRow.state, remoteRow.updated_at || \"\")) {\n      localRevision = Math.max(1, localRevision);\n      queueSupabaseSave(0);\n      if (syncStatus) syncStatus.textContent = \"Pushing newer local changes\u2026\";\n      return;\n    }\n\n    if (localRevision > syncedRevision) return;\n\n    applyRemoteState(\n      remoteRow.state,\n      \"Synced with Supabase.\",\n      { updatedAt: remoteRow.updated_at || \"\" }\n    );\n  };\n\n  // If a write fails and the page stays open, periodically retry while there\n  // are unsynced local revisions.\n  setInterval(() => {\n    if (!supabaseClient || supabaseSaveInFlight || saveTimer) return;\n    if (localRevision > syncedRevision) queueSupabaseSave(0);\n  }, 5000);\n})();\n";
    (0, eval)(source + "\n//# sourceURL=locked-os-pinned-app-with-sync-fix.js");
  } catch (error) {
    console.error("LOCKED OS: could not load the pinned app.js base file.", error);
    document.addEventListener("DOMContentLoaded", () => {
      const status = document.getElementById("syncStatus");
      if (status) status.textContent = "App load failed. Check internet/CDN access.";
    });
  }
})();
